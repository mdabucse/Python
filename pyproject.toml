[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asynco>=0.0.5",
    "mysql-connector-python>=9.6.0",
    "playwright>=1.58.0",
]
